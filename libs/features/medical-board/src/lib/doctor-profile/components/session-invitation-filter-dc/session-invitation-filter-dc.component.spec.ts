/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SessionInvitationFilterDcComponent } from './session-invitation-filter-dc.component';

describe('SessionInvitationFilterDcComponent', () => {
  let component: SessionInvitationFilterDcComponent;
  let fixture: ComponentFixture<SessionInvitationFilterDcComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ SessionInvitationFilterDcComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(SessionInvitationFilterDcComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
