/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TransactionRetirementLumpsumScComponent } from './transaction-retirement-lumpsum-sc.component';

describe('TransactionRetirementLumpsumScComponent', () => {
  let component: TransactionRetirementLumpsumScComponent;
  let fixture: ComponentFixture<TransactionRetirementLumpsumScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TransactionRetirementLumpsumScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TransactionRetirementLumpsumScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
