/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ValidatorModifyCommitmentScComponent } from './validator-modify-commitment-sc.component';

describe('ValidatorModifyCommitmentScComponent', () => {
  let component: ValidatorModifyCommitmentScComponent;
  let fixture: ComponentFixture<ValidatorModifyCommitmentScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ValidatorModifyCommitmentScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ValidatorModifyCommitmentScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
