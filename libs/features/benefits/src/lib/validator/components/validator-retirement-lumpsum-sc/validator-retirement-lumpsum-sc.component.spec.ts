/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ValidatorRetirementLumpsumScComponent } from './validator-retirement-lumpsum-sc.component';

describe('ValidatorRetirementLumpsumScComponent', () => {
  let component: ValidatorRetirementLumpsumScComponent;
  let fixture: ComponentFixture<ValidatorRetirementLumpsumScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ValidatorRetirementLumpsumScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ValidatorRetirementLumpsumScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
