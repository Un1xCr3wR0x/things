/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PreviousAssessmentsDcComponent } from './previous-assessments-dc.component';

describe('PreviousAssessmentsDcComponent', () => {
  let component: PreviousAssessmentsDcComponent;
  let fixture: ComponentFixture<PreviousAssessmentsDcComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PreviousAssessmentsDcComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(PreviousAssessmentsDcComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
