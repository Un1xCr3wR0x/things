import { BilingualText, GosiCalendar } from "@gosi-ui/core";

export class REngagement{
        crmid?: number=undefined;
        reactivateReason?: BilingualText = new BilingualText();   
        editFlow?: boolean=false;     
}