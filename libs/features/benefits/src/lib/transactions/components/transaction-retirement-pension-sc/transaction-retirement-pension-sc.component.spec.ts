/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TransactionRetirementPensionScComponent } from './transaction-retirement-pension-sc.component';

describe('TransactionRetirementPensionScComponent', () => {
  let component: TransactionRetirementPensionScComponent;
  let fixture: ComponentFixture<TransactionRetirementPensionScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TransactionRetirementPensionScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TransactionRetirementPensionScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
