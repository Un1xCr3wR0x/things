/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ValidatorReturnLumpsumScComponent } from './validator-return-lumpsum-sc.component';

describe('ValidatorReturnLumpsumScComponent', () => {
  let component: ValidatorReturnLumpsumScComponent;
  let fixture: ComponentFixture<ValidatorReturnLumpsumScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ValidatorReturnLumpsumScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ValidatorReturnLumpsumScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
