/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TransactionFuneralGrantScComponent } from './transaction-funeral-grant-sc.component';

describe('TransactionFuneralGrantScComponent', () => {
  let component: TransactionFuneralGrantScComponent;
  let fixture: ComponentFixture<TransactionFuneralGrantScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TransactionFuneralGrantScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TransactionFuneralGrantScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
