/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ValidatorRejoiningScComponent } from './validator-rejoining-sc.component';

describe('ValidatorRejoiningScComponent', () => {
  let component: ValidatorRejoiningScComponent;
  let fixture: ComponentFixture<ValidatorRejoiningScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ValidatorRejoiningScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ValidatorRejoiningScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
