/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TransactionRejoiningScComponent } from './transaction-rejoining-sc.component';

describe('TransactionRejoiningScComponent', () => {
  let component: TransactionRejoiningScComponent;
  let fixture: ComponentFixture<TransactionRejoiningScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TransactionRejoiningScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TransactionRejoiningScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
