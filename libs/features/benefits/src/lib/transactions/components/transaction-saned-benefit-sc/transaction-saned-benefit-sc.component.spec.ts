/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TransactionSanedBenefitScComponent } from './transaction-saned-benefit-sc.component';

describe('TransactionSanedBenefitScComponent', () => {
  let component: TransactionSanedBenefitScComponent;
  let fixture: ComponentFixture<TransactionSanedBenefitScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TransactionSanedBenefitScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TransactionSanedBenefitScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
