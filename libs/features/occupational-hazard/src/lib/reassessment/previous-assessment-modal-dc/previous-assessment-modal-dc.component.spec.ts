/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PreviousAssessmentModalDcComponent } from './previous-assessment-modal-dc.component';

describe('PreviousAssessmentModalDcComponent', () => {
  let component: PreviousAssessmentModalDcComponent;
  let fixture: ComponentFixture<PreviousAssessmentModalDcComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [PreviousAssessmentModalDcComponent]
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(PreviousAssessmentModalDcComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
