/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ValidatorRetirementPensionScComponent } from './validator-retirement-pension-sc.component';

describe('ValidatorRetirementPensionScComponent', () => {
  let component: ValidatorRetirementPensionScComponent;
  let fixture: ComponentFixture<ValidatorRetirementPensionScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ValidatorRetirementPensionScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ValidatorRetirementPensionScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
