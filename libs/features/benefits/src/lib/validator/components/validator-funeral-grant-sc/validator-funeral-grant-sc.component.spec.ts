/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ValidatorFuneralGrantScComponent } from './validator-funeral-grant-sc.component';

describe('ValidatorFuneralGrantScComponent', () => {
  let component: ValidatorFuneralGrantScComponent;
  let fixture: ComponentFixture<ValidatorFuneralGrantScComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ValidatorFuneralGrantScComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ValidatorFuneralGrantScComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
